package templates

import "github.com/felipefbs/portfolio/portfolio"

templ AboutContent(educationList []*portfolio.EducationItemProps) {
	<section class="mb-16">
		<div class="flex flex-col md:flex-row gap-8 items-start">
			<img
				src="/static/images/felipe_ferreira.jpeg"
				alt="Felipe Ferreira"
				class="w-32 h-32 rounded-2xl object-cover shadow-lg"
			/>
			<div>
				<h1 class="text-4xl font-bold text-zinc-100 mb-2">Felipe Ferreira</h1>
				<p class="text-emerald-400 font-medium mb-4">Desenvolvedor Backend</p>
				<div class="flex gap-4">
					@SocialIcon("https://github.com/felipefbs", "GitHub")
					@SocialIcon("https://linkedin.com/in/felipefbs", "LinkedIn")
				</div>
			</div>
		</div>
	</section>

	<section class="mb-16">
		<p class="text-lg text-zinc-400 leading-relaxed">
			Desenvolvedor Backend com foco em Go e arquitetura de microsserviços.
			Experiência com metodologias ágeis, TDD e modelagem de domínio.
			Graduado em Telemática pelo IFPB.
		</p>
	</section>

	<section>
		<h2 class="text-xl font-semibold text-zinc-100 mb-6">Formação</h2>
		<div class="space-y-4">
			for _, item := range educationList {
				@Education(item)
			}
		</div>
	</section>
}

templ SocialIcon(url, name string) {
	<a
		href={ templ.SafeURL(url) }
		target="_blank"
		class="px-4 py-2 bg-zinc-900 hover:bg-zinc-800 text-zinc-300 hover:text-zinc-100 rounded-lg text-sm font-medium transition-colors border border-zinc-800 hover:border-zinc-700"
	>
		{ name }
	</a>
}

templ Education(item *portfolio.EducationItemProps) {
	<div class="p-4 bg-zinc-900/50 rounded-lg border border-zinc-800 hover:border-zinc-700 transition-colors">
		<div class="flex flex-col md:flex-row md:items-center md:justify-between gap-2">
			<div>
				<span class="text-xs font-medium text-emerald-400 uppercase tracking-wider">{ item.Type }</span>
				<h3 class="text-zinc-100 font-semibold">{ item.Title }</h3>
				<a href={ templ.URL(item.OrganizationLink) } target="_blank" class="text-zinc-400 hover:text-zinc-300 text-sm">
					{ item.Organization }
				</a>
			</div>
			<div class="text-right">
				<span class="text-zinc-500 text-sm">{ item.Duration }</span>
				<p class="text-zinc-500 text-sm">{ item.Location }</p>
			</div>
		</div>
	</div>
}
